// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.Treasury {
    public var receivedCredits: ReceivedCredits {
        ReceivedCredits(path: path + "/received_credits")
    }

    public struct ReceivedCredits {
        /// Path: `/v1/treasury/received_credits`
        public let path: String

        /// <p>Returns a list of ReceivedCredits.</p>
        public func get(parameters: GetParameters) -> Request<GetResponse> {
            Request(method: "GET", url: path, query: parameters.asQuery, id: "GetTreasuryReceivedCredits")
        }

        /// TreasuryReceivedCreditsResourceReceivedCreditList
        public struct GetResponse: Decodable {
            /// Details about each object.
            public var data: [StripeKit.TreasuryReceivedCredit]
            /// True if this list has another page of items after this one that can be fetched.
            public var hasMore: Bool
            /// String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
            public var object: Object
            /// The URL where this list can be accessed.
            public var url: String

            /// String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
            public enum Object: String, Codable, CaseIterable {
                case list
            }

            public init(data: [StripeKit.TreasuryReceivedCredit], hasMore: Bool, object: Object, url: String) {
                self.data = data
                self.hasMore = hasMore
                self.object = object
                self.url = url
            }

            public init(from decoder: Decoder) throws {
                let values = try decoder.container(keyedBy: StringCodingKey.self)
                self.data = try values.decode([StripeKit.TreasuryReceivedCredit].self, forKey: "data")
                self.hasMore = try values.decode(Bool.self, forKey: "has_more")
                self.object = try values.decode(Object.self, forKey: "object")
                self.url = try values.decode(String.self, forKey: "url")
            }
        }

        public struct GetParameters {
            public var endingBefore: String?
            public var expand: [String]?
            public var financialAccount: String
            public var limit: Int?
            /// Linked_flows_param
            public var linkedFlows: LinkedFlows?
            public var startingAfter: String?
            public var status: Status?

            /// Linked_flows_param
            public struct LinkedFlows: Codable {
                public var sourceFlowType: SourceFlowType

                public enum SourceFlowType: String, Codable, CaseIterable {
                    case creditReversal = "credit_reversal"
                    case other
                    case outboundPayment = "outbound_payment"
                    case payout
                }

                public init(sourceFlowType: SourceFlowType) {
                    self.sourceFlowType = sourceFlowType
                }

                public var asQuery: [(String, String?)] {
                    let encoder = URLQueryEncoder()
                    encoder.encode(sourceFlowType, forKey: "source_flow_type")
                    return encoder.items
                }
            }

            public enum Status: String, Codable, CaseIterable {
                case failed
                case succeeded
            }

            public init(endingBefore: String? = nil, expand: [String]? = nil, financialAccount: String, limit: Int? = nil, linkedFlows: LinkedFlows? = nil, startingAfter: String? = nil, status: Status? = nil) {
                self.endingBefore = endingBefore
                self.expand = expand
                self.financialAccount = financialAccount
                self.limit = limit
                self.linkedFlows = linkedFlows
                self.startingAfter = startingAfter
                self.status = status
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(endingBefore, forKey: "ending_before")
                encoder.encode(expand, forKey: "expand", isDeepObject: true)
                encoder.encode(financialAccount, forKey: "financial_account")
                encoder.encode(limit, forKey: "limit")
                encoder.encode(linkedFlows, forKey: "linked_flows", isDeepObject: true)
                encoder.encode(startingAfter, forKey: "starting_after")
                encoder.encode(status, forKey: "status")
                return encoder.items
            }
        }
    }
}
