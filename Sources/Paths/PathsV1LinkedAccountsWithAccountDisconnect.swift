// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.LinkedAccounts.WithAccount {
    public var disconnect: Disconnect {
        Disconnect(path: path + "/disconnect")
    }

    public struct Disconnect {
        /// Path: `/v1/linked_accounts/{account}/disconnect`
        public let path: String

        /// <p>Disables your access to a Financial Connections <code>Account</code>. You will no longer be able to access data associated with the account (e.g. balances, transactions).</p>
        public func post(_ body: PostRequest? = nil) -> Request<StripeKit.FinancialConnectionsAccount> {
            Request(method: "POST", url: path, body: body.map(URLQueryEncoder.encode)?.percentEncodedQuery, id: "PostLinkedAccountsAccountDisconnect")
        }

        public struct PostRequest: Encodable {
            /// Specifies which fields in the response should be expanded.
            public var expand: [String]?

            public init(expand: [String]? = nil) {
                self.expand = expand
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(expand, forKey: "expand")
                return encoder.items
            }
        }
    }
}
