// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.FinancialConnections.Accounts.WithAccount {
    public var refresh: Refresh {
        Refresh(path: path + "/refresh")
    }

    public struct Refresh {
        /// Path: `/v1/financial_connections/accounts/{account}/refresh`
        public let path: String

        /// <p>Refreshes the data associated with a Financial Connections <code>Account</code>.</p>
        public func post(_ body: PostRequest) -> Request<StripeKit.FinancialConnectionsAccount> {
            Request(method: "POST", url: path, body: URLQueryEncoder.encode(body).percentEncodedQuery, id: "PostFinancialConnectionsAccountsAccountRefresh")
        }

        public struct PostRequest: Encodable {
            /// Specifies which fields in the response should be expanded.
            public var expand: [String]?
            /// The list of account features that you would like to refresh. Either: `balance` or `ownership`.
            public var features: [Feature]

            public enum Feature: String, Codable, CaseIterable {
                case balance
                case ownership
            }

            public init(expand: [String]? = nil, features: [Feature]) {
                self.expand = expand
                self.features = features
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(expand, forKey: "expand")
                encoder.encode(features, forKey: "features")
                return encoder.items
            }
        }
    }
}
