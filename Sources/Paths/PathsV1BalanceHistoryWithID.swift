// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.Balance.History {
    public func id(_ id: String) -> WithID {
        WithID(path: "\(path)/\(id)")
    }

    public struct WithID {
        /// Path: `/v1/balance/history/{id}`
        public let path: String

        /// <p>Retrieves the balance transaction with the given ID.</p>
        /// 
        /// <p>Note that this endpoint previously used the path <code>/v1/balance/history/:id</code>.</p>
        public func get(expand: [String]? = nil) -> Request<StripeKit.BalanceTransaction> {
            Request(method: "GET", url: path, query: makeGetQuery(expand), id: "GetBalanceHistoryId")
        }

        private func makeGetQuery(_ expand: [String]?) -> [(String, String?)] {
            let encoder = URLQueryEncoder()
            encoder.encode(expand, forKey: "expand", isDeepObject: true)
            return encoder.items
        }
    }
}
