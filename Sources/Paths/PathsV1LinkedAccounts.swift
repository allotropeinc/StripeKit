// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths {
    public static var linkedAccounts: LinkedAccounts {
        LinkedAccounts(path: "/v1/linked_accounts")
    }

    public struct LinkedAccounts {
        /// Path: `/v1/linked_accounts`
        public let path: String

        /// <p>Returns a list of Financial Connections <code>Account</code> objects.</p>
        public func get(parameters: GetParameters? = nil) -> Request<GetResponse> {
            Request(method: "GET", url: path, query: parameters?.asQuery, id: "GetLinkedAccounts")
        }

        /// BankConnectionsResourceLinkedAccountList
        public struct GetResponse: Decodable {
            /// Details about each object.
            public var data: [StripeKit.FinancialConnectionsAccount]
            /// True if this list has another page of items after this one that can be fetched.
            public var hasMore: Bool
            /// String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
            public var object: Object
            /// The URL where this list can be accessed.
            public var url: String

            /// String representing the object's type. Objects of the same type share the same value. Always has the value `list`.
            public enum Object: String, Codable, CaseIterable {
                case list
            }

            public init(data: [StripeKit.FinancialConnectionsAccount], hasMore: Bool, object: Object, url: String) {
                self.data = data
                self.hasMore = hasMore
                self.object = object
                self.url = url
            }

            public init(from decoder: Decoder) throws {
                let values = try decoder.container(keyedBy: StringCodingKey.self)
                self.data = try values.decode([StripeKit.FinancialConnectionsAccount].self, forKey: "data")
                self.hasMore = try values.decode(Bool.self, forKey: "has_more")
                self.object = try values.decode(Object.self, forKey: "object")
                self.url = try values.decode(String.self, forKey: "url")
            }
        }

        public struct GetParameters {
            /// Accountholder_params
            public var accountHolder: AccountHolder?
            public var endingBefore: String?
            public var expand: [String]?
            public var limit: Int?
            public var session: String?
            public var startingAfter: String?

            /// Accountholder_params
            public struct AccountHolder: Codable {
                public var account: String?
                public var customer: String?

                public init(account: String? = nil, customer: String? = nil) {
                    self.account = account
                    self.customer = customer
                }

                public var asQuery: [(String, String?)] {
                    let encoder = URLQueryEncoder()
                    encoder.encode(account, forKey: "account")
                    encoder.encode(customer, forKey: "customer")
                    return encoder.items
                }
            }

            public init(accountHolder: AccountHolder? = nil, endingBefore: String? = nil, expand: [String]? = nil, limit: Int? = nil, session: String? = nil, startingAfter: String? = nil) {
                self.accountHolder = accountHolder
                self.endingBefore = endingBefore
                self.expand = expand
                self.limit = limit
                self.session = session
                self.startingAfter = startingAfter
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(accountHolder, forKey: "account_holder", isDeepObject: true)
                encoder.encode(endingBefore, forKey: "ending_before")
                encoder.encode(expand, forKey: "expand", isDeepObject: true)
                encoder.encode(limit, forKey: "limit")
                encoder.encode(session, forKey: "session")
                encoder.encode(startingAfter, forKey: "starting_after")
                return encoder.items
            }
        }
    }
}
