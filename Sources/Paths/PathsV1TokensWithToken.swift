// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.Tokens {
    public func token(_ token: String) -> WithToken {
        WithToken(path: "\(path)/\(token)")
    }

    public struct WithToken {
        /// Path: `/v1/tokens/{token}`
        public let path: String

        /// <p>Retrieves the token with the given ID.</p>
        public func get(expand: [String]? = nil) -> Request<StripeKit.Token> {
            Request(method: "GET", url: path, query: makeGetQuery(expand), id: "GetTokensToken")
        }

        private func makeGetQuery(_ expand: [String]?) -> [(String, String?)] {
            let encoder = URLQueryEncoder()
            encoder.encode(expand, forKey: "expand", isDeepObject: true)
            return encoder.items
        }
    }
}
