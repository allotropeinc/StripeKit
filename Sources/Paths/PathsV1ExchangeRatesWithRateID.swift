// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.ExchangeRates {
    public func rateID(_ rateID: String) -> WithRateID {
        WithRateID(path: "\(path)/\(rateID)")
    }

    public struct WithRateID {
        /// Path: `/v1/exchange_rates/{rate_id}`
        public let path: String

        /// <p>Retrieves the exchange rates from the given currency to every supported currency.</p>
        public func get(expand: [String]? = nil) -> Request<StripeKit.ExchangeRate> {
            Request(method: "GET", url: path, query: makeGetQuery(expand), id: "GetExchangeRatesRateId")
        }

        private func makeGetQuery(_ expand: [String]?) -> [(String, String?)] {
            let encoder = URLQueryEncoder()
            encoder.encode(expand, forKey: "expand", isDeepObject: true)
            return encoder.items
        }
    }
}
