// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.TestHelpers.Treasury.InboundTransfers.WithID {
    public var `return`: Return {
        Return(path: path + "/return")
    }

    public struct Return {
        /// Path: `/v1/test_helpers/treasury/inbound_transfers/{id}/return`
        public let path: String

        /// <p>Marks the test mode InboundTransfer object as returned and links the InboundTransfer to a ReceivedDebit. The InboundTransfer must already be in the <code>succeeded</code> state.</p>
        public func post(_ body: PostRequest? = nil) -> Request<StripeKit.TreasuryInboundTransfer> {
            Request(method: "POST", url: path, body: body.map(URLQueryEncoder.encode)?.percentEncodedQuery, id: "PostTestHelpersTreasuryInboundTransfersIdReturn")
        }

        public struct PostRequest: Encodable {
            /// Specifies which fields in the response should be expanded.
            public var expand: [String]?

            public init(expand: [String]? = nil) {
                self.expand = expand
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(expand, forKey: "expand")
                return encoder.items
            }
        }
    }
}
