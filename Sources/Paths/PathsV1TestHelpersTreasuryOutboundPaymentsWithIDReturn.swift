// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.TestHelpers.Treasury.OutboundPayments.WithID {
    public var `return`: Return {
        Return(path: path + "/return")
    }

    public struct Return {
        /// Path: `/v1/test_helpers/treasury/outbound_payments/{id}/return`
        public let path: String

        /// <p>Transitions a test mode created OutboundPayment to the <code>returned</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
        public func post(_ body: PostRequest? = nil) -> Request<StripeKit.TreasuryOutboundPayment> {
            Request(method: "POST", url: path, body: body.map(URLQueryEncoder.encode)?.percentEncodedQuery, id: "PostTestHelpersTreasuryOutboundPaymentsIdReturn")
        }

        public struct PostRequest: Encodable {
            /// Specifies which fields in the response should be expanded.
            public var expand: [String]?
            /// Returned_details_params
            ///
            /// Optional hash to set the the return code.
            public var returnedDetails: ReturnedDetails?

            /// Returned_details_params
            ///
            /// Optional hash to set the the return code.
            public struct ReturnedDetails: Encodable {
                public var code: Code?

                public enum Code: String, Codable, CaseIterable {
                    case accountClosed = "account_closed"
                    case accountFrozen = "account_frozen"
                    case bankAccountRestricted = "bank_account_restricted"
                    case bankOwnershipChanged = "bank_ownership_changed"
                    case declined
                    case incorrectAccountHolderName = "incorrect_account_holder_name"
                    case invalidAccountNumber = "invalid_account_number"
                    case invalidCurrency = "invalid_currency"
                    case noAccount = "no_account"
                    case other
                }

                public init(code: Code? = nil) {
                    self.code = code
                }

                public var asQuery: [(String, String?)] {
                    let encoder = URLQueryEncoder()
                    encoder.encode(code, forKey: "code")
                    return encoder.items
                }
            }

            public init(expand: [String]? = nil, returnedDetails: ReturnedDetails? = nil) {
                self.expand = expand
                self.returnedDetails = returnedDetails
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(expand, forKey: "expand")
                encoder.encode(returnedDetails, forKey: "returned_details")
                return encoder.items
            }
        }
    }
}
