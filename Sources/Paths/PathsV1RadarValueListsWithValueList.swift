// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.Radar.ValueLists {
    public func valueList(_ valueList: String) -> WithValueList {
        WithValueList(path: "\(path)/\(valueList)")
    }

    public struct WithValueList {
        /// Path: `/v1/radar/value_lists/{value_list}`
        public let path: String

        /// <p>Retrieves a <code>ValueList</code> object.</p>
        public func get(expand: [String]? = nil) -> Request<StripeKit.RadarValueList> {
            Request(method: "GET", url: path, query: makeGetQuery(expand), id: "GetRadarValueListsValueList")
        }

        private func makeGetQuery(_ expand: [String]?) -> [(String, String?)] {
            let encoder = URLQueryEncoder()
            encoder.encode(expand, forKey: "expand", isDeepObject: true)
            return encoder.items
        }

        /// <p>Updates a <code>ValueList</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged. Note that <code>item_type</code> is immutable.</p>
        public func post(_ body: PostRequest? = nil) -> Request<StripeKit.RadarValueList> {
            Request(method: "POST", url: path, body: body.map(URLQueryEncoder.encode)?.percentEncodedQuery, id: "PostRadarValueListsValueList")
        }

        public struct PostRequest: Encodable {
            /// The name of the value list for use in rules.
            public var alias: String?
            /// Specifies which fields in the response should be expanded.
            public var expand: [String]?
            /// Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
            public var metadata: [String: String]?
            /// The human-readable name of the value list.
            public var name: String?

            public init(alias: String? = nil, expand: [String]? = nil, metadata: [String: String]? = nil, name: String? = nil) {
                self.alias = alias
                self.expand = expand
                self.metadata = metadata
                self.name = name
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(alias, forKey: "alias")
                encoder.encode(expand, forKey: "expand")
                encoder.encode(metadata, forKey: "metadata")
                encoder.encode(name, forKey: "name")
                return encoder.items
            }
        }

        /// <p>Deletes a <code>ValueList</code> object, also deleting any items contained within the value list. To be deleted, a value list must not be referenced in any rules.</p>
        public var delete: Request<StripeKit.DeletedRadarValueList> {
            Request(method: "DELETE", url: path, id: "DeleteRadarValueListsValueList")
        }
    }
}
