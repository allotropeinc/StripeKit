// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.CreditNotes {
    public func id(_ id: String) -> WithID {
        WithID(path: "\(path)/\(id)")
    }

    public struct WithID {
        /// Path: `/v1/credit_notes/{id}`
        public let path: String

        /// <p>Retrieves the credit note object with the given identifier.</p>
        public func get(expand: [String]? = nil) -> Request<StripeKit.CreditNote> {
            Request(method: "GET", url: path, query: makeGetQuery(expand), id: "GetCreditNotesId")
        }

        private func makeGetQuery(_ expand: [String]?) -> [(String, String?)] {
            let encoder = URLQueryEncoder()
            encoder.encode(expand, forKey: "expand", isDeepObject: true)
            return encoder.items
        }

        /// <p>Updates an existing credit note.</p>
        public func post(_ body: PostRequest? = nil) -> Request<StripeKit.CreditNote> {
            Request(method: "POST", url: path, body: body.map(URLQueryEncoder.encode)?.percentEncodedQuery, id: "PostCreditNotesId")
        }

        public struct PostRequest: Encodable {
            /// Specifies which fields in the response should be expanded.
            public var expand: [String]?
            /// Credit note memo.
            public var memo: String?
            /// Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
            public var metadata: [String: String]?

            public init(expand: [String]? = nil, memo: String? = nil, metadata: [String: String]? = nil) {
                self.expand = expand
                self.memo = memo
                self.metadata = metadata
            }

            public var asQuery: [(String, String?)] {
                let encoder = URLQueryEncoder()
                encoder.encode(expand, forKey: "expand")
                encoder.encode(memo, forKey: "memo")
                encoder.encode(metadata, forKey: "metadata")
                return encoder.items
            }
        }
    }
}
