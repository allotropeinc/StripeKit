// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation
import Get
import URLQueryEncoder

extension Paths.Checkout.Sessions {
    public func session(_ session: String) -> WithSession {
        WithSession(path: "\(path)/\(session)")
    }

    public struct WithSession {
        /// Path: `/v1/checkout/sessions/{session}`
        public let path: String

        /// <p>Retrieves a Session object.</p>
        public func get(expand: [String]? = nil) -> Request<StripeKit.CheckoutSession> {
            Request(method: "GET", url: path, query: makeGetQuery(expand), id: "GetCheckoutSessionsSession")
        }

        private func makeGetQuery(_ expand: [String]?) -> [(String, String?)] {
            let encoder = URLQueryEncoder()
            encoder.encode(expand, forKey: "expand", isDeepObject: true)
            return encoder.items
        }
    }
}
