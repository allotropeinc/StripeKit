// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct EmailSent: Codable {
    /// The timestamp when the email was sent.
    public var emailSentAt: Int
    /// The recipient's email address.
    public var emailSentTo: String

    public init(emailSentAt: Int, emailSentTo: String) {
        self.emailSentAt = emailSentAt
        self.emailSentTo = emailSentTo
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.emailSentAt = try values.decode(Int.self, forKey: "email_sent_at")
        self.emailSentTo = try values.decode(String.self, forKey: "email_sent_to")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(emailSentAt, forKey: "email_sent_at")
        try values.encode(emailSentTo, forKey: "email_sent_to")
    }
}
