// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct PaymentLinksResourceConsentCollection: Codable {
    /// If set to `auto`, enables the collection of customer consent for promotional communications.
    public var promotions: Promotions?

    /// If set to `auto`, enables the collection of customer consent for promotional communications.
    public enum Promotions: String, Codable, CaseIterable {
        case auto
        case `none`
    }

    public init(promotions: Promotions? = nil) {
        self.promotions = promotions
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.promotions = try values.decodeIfPresent(Promotions.self, forKey: "promotions")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(promotions, forKey: "promotions")
    }
}
