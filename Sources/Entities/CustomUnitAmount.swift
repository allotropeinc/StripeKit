// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct CustomUnitAmount: Codable {
    /// The maximum unit amount the customer can specify for this item.
    public var maximum: Int?
    /// The minimum unit amount the customer can specify for this item. Must be at least the minimum charge amount.
    public var minimum: Int?
    /// The starting unit amount which can be updated by the customer.
    public var preset: Int?

    public init(maximum: Int? = nil, minimum: Int? = nil, preset: Int? = nil) {
        self.maximum = maximum
        self.minimum = minimum
        self.preset = preset
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.maximum = try values.decodeIfPresent(Int.self, forKey: "maximum")
        self.minimum = try values.decodeIfPresent(Int.self, forKey: "minimum")
        self.preset = try values.decodeIfPresent(Int.self, forKey: "preset")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(maximum, forKey: "maximum")
        try values.encodeIfPresent(minimum, forKey: "minimum")
        try values.encodeIfPresent(preset, forKey: "preset")
    }
}
