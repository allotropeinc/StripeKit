// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct PaymentIntentProcessing: Codable {
    /// PaymentIntentCardProcessing
    public var card: PaymentIntentCardProcessing?
    /// Type of the payment method for which payment is in `processing` state, one of `card`.
    public var type: `Type`

    /// Type of the payment method for which payment is in `processing` state, one of `card`.
    public enum `Type`: String, Codable, CaseIterable {
        case card
    }

    public init(card: PaymentIntentCardProcessing? = nil, type: `Type`) {
        self.card = card
        self.type = type
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.card = try values.decodeIfPresent(PaymentIntentCardProcessing.self, forKey: "card")
        self.type = try values.decode(`Type`.self, forKey: "type")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(card, forKey: "card")
        try values.encode(type, forKey: "type")
    }
}
