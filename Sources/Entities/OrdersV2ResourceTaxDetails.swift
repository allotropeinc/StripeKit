// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct OrdersV2ResourceTaxDetails: Codable {
    /// Describes the purchaser's tax exemption status. One of `none`, `exempt`, or `reverse`.
    public var taxExempt: TaxExempt
    /// The purchaser's tax IDs to be used in calculation of tax for this Order.
    public var taxIDs: [OrdersV2ResourceTaxDetailsResourceTaxID]

    /// Describes the purchaser's tax exemption status. One of `none`, `exempt`, or `reverse`.
    public enum TaxExempt: String, Codable, CaseIterable {
        case exempt
        case `none`
        case reverse
    }

    public init(taxExempt: TaxExempt, taxIDs: [OrdersV2ResourceTaxDetailsResourceTaxID]) {
        self.taxExempt = taxExempt
        self.taxIDs = taxIDs
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.taxExempt = try values.decode(TaxExempt.self, forKey: "tax_exempt")
        self.taxIDs = try values.decode([OrdersV2ResourceTaxDetailsResourceTaxID].self, forKey: "tax_ids")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(taxExempt, forKey: "tax_exempt")
        try values.encode(taxIDs, forKey: "tax_ids")
    }
}
