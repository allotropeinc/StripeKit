// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct QuotesResourceComputed: Codable {
    /// The definitive totals and line items the customer will be charged on a recurring basis. Takes into account the line items with recurring prices and discounts with `duration=forever` coupons only. Defaults to `null` if no inputted line items with recurring prices.
    public var recurring: QuotesResourceRecurring?
    /// QuotesResourceUpfront
    public var upfront: QuotesResourceUpfront

    public init(recurring: QuotesResourceRecurring? = nil, upfront: QuotesResourceUpfront) {
        self.recurring = recurring
        self.upfront = upfront
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.recurring = try values.decodeIfPresent(QuotesResourceRecurring.self, forKey: "recurring")
        self.upfront = try values.decode(QuotesResourceUpfront.self, forKey: "upfront")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(recurring, forKey: "recurring")
        try values.encode(upfront, forKey: "upfront")
    }
}
