// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct PortalCustomerUpdate: Codable {
    /// The types of customer updates that are supported. When empty, customers are not updateable.
    public var allowedUpdates: [AllowedUpdate]
    /// Whether the feature is enabled.
    public var isEnabled: Bool

    public enum AllowedUpdate: String, Codable, CaseIterable {
        case address
        case email
        case phone
        case shipping
        case taxID = "tax_id"
    }

    public init(allowedUpdates: [AllowedUpdate], isEnabled: Bool) {
        self.allowedUpdates = allowedUpdates
        self.isEnabled = isEnabled
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.allowedUpdates = try values.decode([AllowedUpdate].self, forKey: "allowed_updates")
        self.isEnabled = try values.decode(Bool.self, forKey: "enabled")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(allowedUpdates, forKey: "allowed_updates")
        try values.encode(isEnabled, forKey: "enabled")
    }
}
