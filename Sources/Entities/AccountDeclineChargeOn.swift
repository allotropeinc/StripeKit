// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct AccountDeclineChargeOn: Codable {
    /// Whether Stripe automatically declines charges with an incorrect ZIP or postal code. This setting only applies when a ZIP or postal code is provided and they fail bank verification.
    public var isAvsFailure: Bool
    /// Whether Stripe automatically declines charges with an incorrect CVC. This setting only applies when a CVC is provided and it fails bank verification.
    public var isCvcFailure: Bool

    public init(isAvsFailure: Bool, isCvcFailure: Bool) {
        self.isAvsFailure = isAvsFailure
        self.isCvcFailure = isCvcFailure
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.isAvsFailure = try values.decode(Bool.self, forKey: "avs_failure")
        self.isCvcFailure = try values.decode(Bool.self, forKey: "cvc_failure")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(isAvsFailure, forKey: "avs_failure")
        try values.encode(isCvcFailure, forKey: "cvc_failure")
    }
}
