// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct ShippingRateCurrencyOption: Codable {
    /// A non-negative integer in cents representing how much to charge.
    public var amount: Int
    /// Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
    public var taxBehavior: TaxBehavior

    /// Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
    public enum TaxBehavior: String, Codable, CaseIterable {
        case exclusive
        case inclusive
        case unspecified
    }

    public init(amount: Int, taxBehavior: TaxBehavior) {
        self.amount = amount
        self.taxBehavior = taxBehavior
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.amount = try values.decode(Int.self, forKey: "amount")
        self.taxBehavior = try values.decode(TaxBehavior.self, forKey: "tax_behavior")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(amount, forKey: "amount")
        try values.encode(taxBehavior, forKey: "tax_behavior")
    }
}
