// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct PromotionCodeCurrencyOption: Codable {
    /// Minimum amount required to redeem this Promotion Code into a Coupon (e.g., a purchase must be $100 or more to work).
    public var minimumAmount: Int

    public init(minimumAmount: Int) {
        self.minimumAmount = minimumAmount
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.minimumAmount = try values.decode(Int.self, forKey: "minimum_amount")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(minimumAmount, forKey: "minimum_amount")
    }
}
