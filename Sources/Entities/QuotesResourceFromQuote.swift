// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct QuotesResourceFromQuote: Codable {
    /// Whether this quote is a revision of a different quote.
    public var isRevision: Bool
    /// The quote that was cloned.
    public var quote: Quote

    /// The quote that was cloned.
    public final class Quote: Codable {
        public let string: String?
        /// A Quote is a way to model prices that you'd like to provide to a customer.
        /// Once accepted, it will automatically create an invoice, subscription or subscription schedule.
        public let quote: StripeKit.Quote?

        public init(string: String? = nil, quote: StripeKit.Quote? = nil) {
            self.string = string
            self.quote = quote
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            self.string = try? container.decode(String.self)
            self.quote = try? container.decode(StripeKit.Quote.self)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            if let value = string { try container.encode(value) }
            if let value = quote { try container.encode(value) }
        }
    }

    public init(isRevision: Bool, quote: Quote) {
        self.isRevision = isRevision
        self.quote = quote
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.isRevision = try values.decode(Bool.self, forKey: "is_revision")
        self.quote = try values.decode(Quote.self, forKey: "quote")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(isRevision, forKey: "is_revision")
        try values.encode(quote, forKey: "quote")
    }
}
