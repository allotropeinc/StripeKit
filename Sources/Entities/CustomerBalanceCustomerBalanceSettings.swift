// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct CustomerBalanceCustomerBalanceSettings: Codable {
    /// The configuration for how funds that land in the customer cash balance are reconciled.
    public var reconciliationMode: ReconciliationMode

    /// The configuration for how funds that land in the customer cash balance are reconciled.
    public enum ReconciliationMode: String, Codable, CaseIterable {
        case automatic
        case manual
    }

    public init(reconciliationMode: ReconciliationMode) {
        self.reconciliationMode = reconciliationMode
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.reconciliationMode = try values.decode(ReconciliationMode.self, forKey: "reconciliation_mode")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(reconciliationMode, forKey: "reconciliation_mode")
    }
}
