// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct TransformQuantity: Codable {
    /// Divide usage by this number.
    public var divideBy: Int
    /// After division, either round the result `up` or `down`.
    public var round: Round

    /// After division, either round the result `up` or `down`.
    public enum Round: String, Codable, CaseIterable {
        case down
        case up
    }

    public init(divideBy: Int, round: Round) {
        self.divideBy = divideBy
        self.round = round
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.divideBy = try values.decode(Int.self, forKey: "divide_by")
        self.round = try values.decode(Round.self, forKey: "round")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(divideBy, forKey: "divide_by")
        try values.encode(round, forKey: "round")
    }
}
