// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct CountrySpecVerificationFieldDetails: Codable {
    /// Additional fields which are only required for some users.
    public var additional: [String]
    /// Fields which every account must eventually provide.
    public var minimum: [String]

    public init(additional: [String], minimum: [String]) {
        self.additional = additional
        self.minimum = minimum
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.additional = try values.decode([String].self, forKey: "additional")
        self.minimum = try values.decode([String].self, forKey: "minimum")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(additional, forKey: "additional")
        try values.encode(minimum, forKey: "minimum")
    }
}
