// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

/// Change to a FinancialAccount's balance
public struct TreasuryTransactionsResourceBalanceImpact: Codable {
    /// The change made to funds the user can spend right now.
    public var cash: Int
    /// The change made to funds that are not spendable yet, but will become available at a later time.
    public var inboundPending: Int
    /// The change made to funds in the account, but not spendable because they are being held for pending outbound flows.
    public var outboundPending: Int

    public init(cash: Int, inboundPending: Int, outboundPending: Int) {
        self.cash = cash
        self.inboundPending = inboundPending
        self.outboundPending = outboundPending
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.cash = try values.decode(Int.self, forKey: "cash")
        self.inboundPending = try values.decode(Int.self, forKey: "inbound_pending")
        self.outboundPending = try values.decode(Int.self, forKey: "outbound_pending")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(cash, forKey: "cash")
        try values.encode(inboundPending, forKey: "inbound_pending")
        try values.encode(outboundPending, forKey: "outbound_pending")
    }
}
