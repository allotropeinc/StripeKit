// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

public struct Networks: Codable {
    /// All available networks for the card.
    public var available: [String]
    /// The preferred network for the card.
    public var preferred: String?

    public init(available: [String], preferred: String? = nil) {
        self.available = available
        self.preferred = preferred
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.available = try values.decode([String].self, forKey: "available")
        self.preferred = try values.decodeIfPresent(String.self, forKey: "preferred")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encode(available, forKey: "available")
        try values.encodeIfPresent(preferred, forKey: "preferred")
    }
}
