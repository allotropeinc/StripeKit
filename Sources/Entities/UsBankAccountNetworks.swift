// Generated by Create API
// https://github.com/CreateAPI/CreateAPI

import Foundation

/// Us_bank_account_networks
public struct UsBankAccountNetworks: Codable {
    /// The preferred network.
    public var preferred: String?
    /// All supported networks.
    public var supported: [SupportedItem]

    public enum SupportedItem: String, Codable, CaseIterable {
        case ach
        case usDomesticWire = "us_domestic_wire"
    }

    public init(preferred: String? = nil, supported: [SupportedItem]) {
        self.preferred = preferred
        self.supported = supported
    }

    public init(from decoder: Decoder) throws {
        let values = try decoder.container(keyedBy: StringCodingKey.self)
        self.preferred = try values.decodeIfPresent(String.self, forKey: "preferred")
        self.supported = try values.decode([SupportedItem].self, forKey: "supported")
    }

    public func encode(to encoder: Encoder) throws {
        var values = encoder.container(keyedBy: StringCodingKey.self)
        try values.encodeIfPresent(preferred, forKey: "preferred")
        try values.encode(supported, forKey: "supported")
    }
}
